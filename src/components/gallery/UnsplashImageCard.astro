---
interface Props {
  id: string
  src: string
  alt?: string
}

const { id, src, alt = 'Unsplash image' } = Astro.props
---

<article class="image-card" data-image-id={id}>
  <img src={src} alt={alt} loading="lazy" />
</article>
